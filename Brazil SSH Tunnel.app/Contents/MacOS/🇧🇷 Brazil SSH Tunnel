#!/bin/bash

# 🇧🇷 Brazil SSH Tunnel - Main Entry Point
# Auto-detect project directory from app location

# Get project directory (app is in project root)
APP_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
PROJECT_DIR="$(dirname "$(dirname "$(dirname "$APP_DIR")")")"

# Open Terminal and auto-start tunnel with quick restart
osascript << APPLESCRIPT
tell application "Terminal"
    activate
    do script "echo '🇧🇷 Brazil SSH Tunnel - Quick Start' && cd '$PROJECT_DIR' && ./tools/start --auto-restart && echo '' && echo '💡 Tip: Close this window or run ./tools/start for the full menu' && echo ''"
    set custom title of front tab of front window to "🇧🇷 Brazil SSH Tunnel"
end tell
APPLESCRIPT
